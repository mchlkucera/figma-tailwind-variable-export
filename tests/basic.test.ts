import { describe, test, expect } from "vitest";

// A very simple test to check if Vitest is working properly
describe("Basic test suite", () => {
   test("Simple test", () => {
      expect(1 + 1).toBe(2);
   });
});
